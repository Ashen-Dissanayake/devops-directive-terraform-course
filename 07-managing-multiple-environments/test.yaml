apiVersion: v1
kind: PersistentVolumeClaim 
metadata:
name: postgres-pc-volume-claim
Labels:
app: postgres
spec:
storageClassName: manual 
accessModes:
  - ReadwriteOnce 
  resources:
    requests:
      storage: 5Gi
